01.Compress the entire project folder on your local machine. You'll get a zip file.

02.Open your cPanel.

03.Click on ‘File Manager’ and Click on Public_html.

04.Upload the compressed zip file and Extract the zip file.

05.copy the all files from public folder and paste to public_html folder.

06.Edit the index.html from public_html. 

	FROM:
		require __DIR__.'/../vendor/autoload.php';
		$app = require_once __DIR__.'/../bootstrap/app.php';
	TO:
		require __DIR__.'/vendor/autoload.php';
		$app = require_once __DIR__.'/bootstrap/app.php';

07.create a database and upload your sql file.

08.Open the .env file from public_html folder and Set your database connection

	DB_DATABASE= (Database name)
	DB_USERNAME= (Database user name)
	DB_PASSWORD= (Database Password)

09.Run your domain in browser url like tagdv.org.